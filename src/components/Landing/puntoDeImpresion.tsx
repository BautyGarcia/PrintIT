import Image from "next/image";

export const PuntodeImpresion: React.FC = ({}) => {
  return (
    <Image
      src="/Landing/PuntodeImpresion.png"
      alt="PuntodeImpresion"
      width={160}
      height={160}
    />
  );
};
