import Image from "next/image";

export const UsoBlack: React.FC = ({}) => {
  return (
    <Image
      src="/Landing/UsoPrintItBlack.png"
      alt="UsoPrintItBlack"
      width={500}
      height={300}
    />
  );
};
