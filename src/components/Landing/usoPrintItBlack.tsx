import Image from "next/image";

export const UsoBlack: React.FC = ({}) => {
  return (
    <Image
      src="/UsoPrintItBlack.png"
      alt="UsoPrintItBlack"
      width={500}
      height={300}
    />
  );
};
